apiVersion: v1
kind: Service
metadata:
  name: kafka-producer
spec:
  type: NodePort
  selector:
    app: kafka-producer
  ports:
    - protocol: TCP
      port: 8081        # service port
      targetPort: 8081  # container port
      nodePort: 30091   # external access port